<configuration>
    <step  setNum="0">
        <class>com.asiainfo.ocdc.streaming.impl.polymerization</class>
        <numTasks>8</numTasks>
        <operationType>count</operationType>
        <groupByKeys>Name,CODCE</groupByKeys>
        <operationKeys>NTcount,Fee</operationKeys>
        <describe>
        1、此操作不与HBASＥ交互，功能等效sql的distinct,sum,count;
        2、 operationType＝[distinct，sum，count];
        3、无需指定输出字段，默认输出为groupByKeys，operationKeys;
        4、只有当operationType＝sum时，operationKeys才会生效；
        5、只有当operationType＝count时，输出字段为groupByKeys和自动生成的字段"［count］"；
        </describe>
    </step>
</configuration>
